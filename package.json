{
  "name": "gitlab-changelog-generator",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register-commands": "bun run scripts/register-commands.ts",
    "typecheck": "tsc --noEmit && tsc --project tsconfig.scripts.json --noEmit",
    "kv:create": "wrangler kv namespace create USERS_KV",
    "queue:create": "wrangler queues create changelog-jobs && wrangler queues create changelog-jobs-dlq",
    "tail": "wrangler tail",
    "logs": "wrangler tail --format=pretty"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "@types/bun": "latest",
    "wrangler": "^4.76.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.12.8"
  }
}
